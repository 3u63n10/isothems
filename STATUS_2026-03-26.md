# Estado del manuscrito

Fecha: 2026-03-26

## Objetivo de esta nota

Guardar el punto exacto en el que quedó el trabajo sobre `main.tex`, `SI.tex` y los notebooks auxiliares.

## Hecho

### 1. Coherencia entre `main.tex` y `SI.tex`

- Se revisaron y corrigieron las referencias cruzadas entre manuscrito principal y SI.
- `SI.tex` compila y no tiene referencias indefinidas.
- Se verifico la correspondencia de labels `SI-*` usados desde `main.tex`.
- Se alinearon las ecuaciones clave entre ambos archivos.

### 2. Correcciones fisico-matematicas

- Sips:
  - Se fijo la expresion de heterogeneidad como
    `sigma_E = pi*RT/(m*sqrt(3))`.
  - Se corrigio el criterio Langmuir vs Sips.
- Double Langmuir:
  - Se paso a usar `K_L^(tot) = (Q1*K1 + Q2*K2)/(Q1+Q2)` para el descriptor global.
  - Se mantuvo la separacion energetica de los dos sitios para la interpretacion fisica.
- Se homogenizo el tratamiento de isotermas en la tabla de extraccion del SI.

### 3. Tabla de disponibilidad en `main.tex`

En `tab:isotherm_availability` se agrego una columna:

- `Recommended use`

Criterio aplicado:

- `YES` en verde si el modelo permite reportar un `Qmax` comparable y un `Kaff` comparable.
- `NO` en rojo si no cumple eso.

### 4. Arbol de decision en `SI.tex`

- Se rehizo el ruteo del `fig:si_decision_tree`.
- Ya no se ven cruces de lineas en la figura renderizada.
- Se separaron mejor las ramas de Sips, Toth, Jovanovic, UNILAN, R-P y Double Langmuir.

### 5. Seccion de errores y comparacion estadistica en `SI.tex`

Se dejo soporte matematico para:

- `SSE`
- `R^2`
- `R^2_adj`
- `EABS`
- `RESID`
- `ARED`
- `MPSED`
- `HYBRID`

Ademas se agregaron ecuaciones para:

- `WSSE`
- `chi^2`
- `chi^2_red`
- `MSE`
- `RMSE`
- varianza residual `s_r^2`
- Jacobiano del modelo
- matriz de covarianza de parametros
- version ponderada de la covarianza
- error estandar de parametros
- intervalos de confianza con `t`
- gradiente de prediccion
- varianza de prediccion
- bandas de confianza
- bootstrap percentil
- indice de sensibilidad al ruido
- relacion entre SSE y log-verosimilitud gaussiana
- AIC, AICc, BIC, Akaike weights, model averaging
- F-test y p-value asociado
- OVL numerico para distribuciones arbitrarias

### 6. Soporte matematico de ML en `SI.tex`

Se reforzo la base de:

- vector de descriptores
- matriz de features
- estandarizacion
- one-hot encoding
- distancia euclidea
- KNN
- LOOCV para KNN
- SVM soft-margin
- forma dual de SVM
- Ward linkage
- validacion por cross-validation
- PCA

### 7. Notebooks y app

Se ajustaron archivos en `notebooks/` para mantener consistencia con el manuscrito:

- `notebooks/utils_isotherms.py`
- `notebooks/app_isotherm_fitting.py`
- `notebooks/README.md`

Tambien se agrego al final de `SI.tex` una seccion de uso del repositorio/notebooks.

## Pendiente

### 1. Bibliografia

- La bibliografia la manejas vos.
- No se cerro `main.tex` a nivel de compilacion final con `.bbl`.
- No se toco el contenido bibliografico.

### 2. Partes no evaluadas por decision explicita

No se evaluaron todavia:

- abstract
- TOC graphic
- conclusions
- acknowledgements

### 3. Posible trabajo futuro de limpieza

Todavia quedan warnings de maquetacion en `SI.tex`:

- `Overfull hbox`
- un `Float too large for page`

No rompen ecuaciones ni referencias, pero se pueden limpiar despues.

## Estado actual por archivo

### `main.tex`

- Coherente con `SI.tex` en formulas clave.
- Tabla `tab:isotherm_availability` ampliada con `Recommended use`.
- Ajustado criterio Langmuir/Sips y extraccion de Double Langmuir.

### `SI.tex`

- Integrado con `main.tex`.
- Sin referencias indefinidas.
- Arbol de decision arreglado.
- Seccion de errores, estadistica y ML expandida.
- Seccion final de notebooks agregada.

### `notebooks/`

- Logica de extraccion de descriptores corregida.
- App de fitting actualizada.
- README actualizado para flujo real de uso.

## Recomendacion para retomar

El siguiente paso razonable, cuando quieras seguir, es uno de estos:

1. limpiar warnings de layout en `SI.tex`
2. revisar ingles y consistencia editorial del `main.tex`
3. terminar de pulir tablas y figuras para envio
4. cerrar compilacion final del principal cuando la bibliografia este lista
